{% layout 'layout.liquid' %}
{% block content %}
    <div class="container-md">
        <article>
        <h2>
        {{ ingredient.name }}
        </h2>
        {% if ingredient.owner == userId %}
        <div class="btn-group">

          <a href="/ingredients/{{ ingredeint.id }}/edit" class="btn btn-primary">Edit</a>
          <form action="/ingredients/{{ ingredient.id }}?_method=DELETE" method="POST">
            <input type="submit" value="Delete" class="btn btn-danger">
          </form>
        </div>
        {% endif %}
        </article>
    </div>
{% endblock %}