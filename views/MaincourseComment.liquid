{% block content %}
<div class = 'comments'>
  <div class="card mb-4 shadow">
    <div class="card-body">
      <p>{{ comment.note }}
      </p>
      <div class="d-flex justify-content-between">
        <small>author:
          {{ comment.author.username }}
        </small>
        {% if comment.author == userId %}
          <form action="/comments/delete/{{maincourse.id}}/{{comment.id}}?_method=DELETE" method="POST">
            <input class="btn btn-secondary" type="submit" value="X">
          </form>
        {% endif %}
      </div>
    </div>
  </div>
</div>
{% endblock %}